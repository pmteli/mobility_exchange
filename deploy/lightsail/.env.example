APP_HOST=test.mobilityexchange.org
# Generate hex-only random values; the DB password is embedded in a URL.
DB_PASSWORD=
SECRET_KEY_BASE=
ENCRYPTION_PRIMARY_KEY=
ENCRYPTION_DETERMINISTIC_KEY=
ENCRYPTION_KEY_DERIVATION_SALT=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_TLS_MODE=starttls
SMTP_AUTHENTICATION=plain
SMTP_USERNAME=admin@mobilityexchange.org
MAIL_FROM=admin@mobilityexchange.org
# Google App Password, never your normal account password.
SMTP_PASSWORD=

# Optional Stripe hosted donations (test keys only; never commit actual secrets)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional Google sign-in. Keep disabled until the OAuth client is configured.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://test.mobilityexchange.org/auth/google/callback
