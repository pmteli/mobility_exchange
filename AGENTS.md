# Mobility Exchange development

Use `Alpha2.0` for all future development, commits, and pushes unless the user explicitly requests a different branch. Before editing, check the current branch and switch to `Alpha2.0` without discarding uncommitted work. Do not merge or push development changes to `main` unless requested.
