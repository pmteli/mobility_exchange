<section class="page"><div class="section-head"><h1>Inventory report</h1><%= link_to "Download CSV", staff_reports_path(format: :csv), class: "button" %></div><div class="grid"><% @totals.each do |status, count| %><div class="card"><%= status_badge(status) %><div class="stat"><%= count %></div></div><% end %></div></section>
